# Tiến độ triển khai — dự án multi-tenant Thôn Đoàn Kết

Theo lộ trình mục 11 của `du-an-quan-ly-thon.md` (`E:\Dev\cong-thong-tin-thon`). File này đặt ở repo BE nhưng theo dõi tiến độ **cả BE lẫn FE**. Cập nhật sau mỗi bước hoàn thành.

## Giai đoạn 1 — Khởi tạo BE NestJS (`E:\Dev\cong-thong-tin-thon-be`) ✅ HOÀN THÀNH

- [x] Project NestJS + MongoDB local (`127.0.0.1:27017`, MongoDB Server 8.0.12, đã có sẵn qua winget)
- [x] Dependencies: `@nestjs/mongoose`, `mongoose@8.24.1` (**không dùng v9**, xem vướng mắc bên dưới), `@nestjs/jwt`, `bcryptjs`, `@nestjs/config`, `@nestjs/throttler`, `class-validator`, `class-transformer`, `dotenv`, `ts-node`
- [x] Mongoose schemas theo ERD (mục 6) — `src/schemas/*.schema.ts`: `Tenant`/`AdministrativeUnit`/`SuperAdmin`, `Account`, `Resident`, `Household`/`VillageFund`, `AssociationQuota`, `HomeContent`, `PermissionMatrix`, `AuditLog`, 3 loại Request, `IncidentReport`/`ResidenceRegistration`
- [x] `TenantModule` — `TenantGuard` đọc `x-tenant-slug`, `GET /tenants/public`
- [x] `AuthModule` — `POST /auth/login`, `GET /auth/me`, `JwtAuthGuard`, `RolesGuard`
- [x] Script migrate (`scripts/migrate-seed.ts`, chạy bằng `ts-node`) — import dữ liệu thật từ `js/data.js` prototype (qua Node `vm` sandbox) + GeoJSON thật từ `tracuudlieya.io.vn` → tenant "doanket" đầy đủ (20 nhân khẩu, 21 tài khoản, GPS hộ, quỹ thôn, 5 chi hội, home content, phân quyền, audit log, 4 trụ sở cơ quan xã)
- [x] `.env`/`.env.example`, build sạch, **đã test thật end-to-end** (xem "Đã kiểm thử" bên dưới)
- [x] **Script sinh thêm hộ/nhân khẩu GIẢ** (`scripts/generate-fake-households.ts`, chạy bằng `ts-node`, lệnh `npm run generate:fake-households`) — `js/data.js` gốc chỉ có dữ liệu chi tiết cho 15 hộ mẫu dù `villageFund.totalHouseholds` ghi sẵn 363 hộ, nên demo trông quá thưa. Script này **bịa thêm** (tên/CCCD/SĐT/ngày sinh ngẫu nhiên hợp lệ, không phải người thật) để đủ 363 hộ: dựng đầy đủ 4 hộ vốn chỉ có tên "đại diện" trong `unpaidHouseholdsList` (FAM-014/027/033/041, giữ đúng tên/ngày sinh/nhóm đã có, đánh dấu đúng khoản chưa đóng 750.000đ như dữ liệu gốc), rồi sinh thêm 344 hộ hoàn toàn giả (FAM-100..FAM-443, 0–3 thành viên/hộ ngoài chủ hộ, GPS rải ngẫu nhiên **có kiểm tra nằm trong ranh giới thật của Thôn Đoàn Kết** bằng thuật toán ray-casting point-in-polygon). Idempotent (dọn đúng dải familyId đã sinh trước khi tạo lại). **Không** tạo Account cho các hộ giả — chỉ 15 hộ mẫu gốc còn tài khoản đăng nhập. **Rủi ro đã biết:** tên sinh ngẫu nhiên từ 1 tập họ/tên cố định nên có ~48 cặp trùng tên hoàn toàn giữa các hộ giả (đã kiểm tra: không trùng với tên bất kỳ tài khoản thật nào tại thời điểm sinh, nên `GET /home-content/public-roster` — vốn nối số điện thoại bằng cách khớp theo tên — chưa bị ảnh hưởng, nhưng đây là kiểu khớp dữ liệu vốn dĩ mong manh, chạy lại script hoặc thêm tài khoản mới nên kiểm tra lại). **Phụ thuộc thứ tự chạy:** phải chạy sau `migrate:seed` (script tự tìm tenant theo slug "doanket" đang có); nếu chạy lại `migrate:seed` sau đó, toàn bộ hộ giả này cũng bị xóa theo (cùng `tenantId` cũ) — cần chạy lại `generate:fake-households` để tái tạo.

### Vướng mắc kỹ thuật đã gặp (đọc trước khi động vào schema/script — tránh lặp lại)

1. **Bug nghiêm trọng đã sửa: `type: Types.ObjectId` sai, phải dùng `type: SchemaTypes.ObjectId`.**
   `import { Types } from 'mongoose'` rồi viết `@Prop({ type: Types.ObjectId, ref: 'Tenant' })` **build được, không lỗi**, nhưng mọi query lọc theo field đó (vd `findOne({ tenantId: '...' })`) luôn trả `null` — vì `@nestjs/mongoose` hiểu nhầm `Types.ObjectId` (class BSON driver) là 1 class con lồng nhau (không phải kiểu Mongoose SchemaType), nên biến field đó thành `Mixed` rỗng (`{}`). Triệu chứng: `findOne({_id: X})` chạy đúng, nhưng `findOne({_id: X, tenantId: Y})` trả `null` dù dữ liệu có thật. **Đã sửa toàn bộ 9 file schema**: import thêm `SchemaTypes` từ `'mongoose'`, dùng `type: SchemaTypes.ObjectId` thay vì `type: Types.ObjectId` (giữ nguyên `Types.ObjectId` cho phần khai báo kiểu TS của property, chỉ đổi trong option `type:`).
2. **`tsx` không chạy được với `@nestjs/mongoose`** (lỗi `CannotDetermineTypeError`) — luôn dùng `ts-node` cho mọi script độc lập trong repo này (`npm run migrate:seed`), không dùng `tsx`. `nest build`/`nest start` (dùng tsc thật) không bị ảnh hưởng.
3. Cookie `secure: true` (khi `NODE_ENV=production`, tức `next start`) sẽ không được trình duyệt/curl gửi lại qua `http://` — chỉ hoạt động qua `https://` thật hoặc khi test bằng `next dev`. Đừng hoảng khi thấy cookie "biến mất" lúc test thủ công qua `curl` ở chế độ production.
4. `TenantDocument` dùng làm type tham số trong method có decorator phải `import type` (TS1272, do `isolatedModules`+`emitDecoratorMetadata`).
5. `JwtModule.registerAsync` cần ép kiểu `expiresIn` (package `ms`, không nhận `string` trần).
6. `categorySlug` tin tức trong `js/data.js` **thật** là `"hanh-chinh"/"san-xuat"/"doan-the"` (không phải `"administrative"/"production"/"community"`).
7. Role/field/level trong `js/data.js` là nhãn tiếng Việt — migration script có `ROLE_MAP`/`FIELD_MAP`/`LEVEL_MAP` để đổi sang slug tiếng Anh.

### Đã kiểm thử end-to-end (curl, cả BE lẫn FE)
`POST /auth/login` (tenant "doanket", `admin`/`doanket`) → nhận JWT → `GET /auth/me` trả đúng tài khoản → FE `/api/auth/login` (BFF) set cookie đúng → FE `/dashboard` (Server Component) gọi `/auth/me` qua cookie, hiển thị "Xin chào, Ban Quản Trị Hệ Thống — admin". Toàn bộ chuỗi subdomain → tenant → JWT → dashboard đã chạy đúng thật, không chỉ build sạch.

## Giai đoạn 2 — Khởi tạo FE Next.js (`E:\Dev\cong-thong-tin-thon-fe`) 🟡 ĐANG LÀM (bước đầu xong)

- [x] Next.js project mới (App Router, TS, Tailwind) — **tên `cong-thong-tin-thon-fe`, không dùng thư mục `cong-thong-tin-thon-next` có sẵn** (theo lựa chọn của bạn, khớp tên trong tài liệu thiết kế)
- [x] `middleware.ts` tách subdomain → header `x-tenant-slug` (hỗ trợ `*.localhost` khi dev, `*.tenxa.vn` khi production)
- [x] `src/lib/tenant.ts`, `src/lib/api.ts` (helper gọi BE kèm tenant header + Bearer cookie)
- [x] Route Handler BFF: `POST /api/auth/login` (set cookie httpOnly), `POST /api/auth/logout`
- [x] Trang `/login` (form thật, gọi BFF), `/dashboard` (Server Component, gọi `/auth/me`, redirect `/login` nếu chưa đăng nhập), `/` (phân biệt có/không có subdomain)
- [x] **Quy ước đặt tên: route/folder dùng tiếng Anh chuẩn** (`login` không phải `dang-nhap`) — xem lưu ý bên dưới
- [x] **Design tokens đồng bộ bản mẫu**: `next/font/google` cho Montserrat (`--font-sans`) + Playfair Display (`--font-serif`, subset `vietnamese` để hiện đúng dấu), palette `primary` (thang xanh lá, y hệt bản mẫu) khai báo qua `@theme` (Tailwind v4) trong `globals.css`; icon dùng `@fortawesome/fontawesome-free` (CSS import trong `layout.tsx`, dùng `<i className="fa-solid ...">` y như bản mẫu — không cần đổi sang React component). Đã copy `logo.png` từ bản mẫu vào `public/`.
- [x] **BE: thêm `HomeContentModule`** (`GET /home-content`, public qua `TenantGuard`) — trả đúng dữ liệu thật đã seed (stats/news/products/security/schedule/gallery), không hardcode ở FE.
- [x] **BE: thêm `GET /home-content/public-roster`** — "Ban Tự Quản"/"Tổ ANTT" không phải nội dung tĩnh, sinh động từ `Account.position`/`Account.role` + số điện thoại từ `Resident` cùng tên (đúng logic `renderHomeLeadership()`/`renderHomeSecurity()` của bản mẫu). Ban đầu bỏ sót 2 mục này khi dựng trang chủ lần đầu — đã bổ sung sau khi so lại với `index.html` gốc.
- [x] **Viết lại layout trang chủ đúng bố cục gốc** (lần đầu chỉ đúng nội dung, sai bố cục — đã sửa theo phản hồi): hero ảnh lớn + Bảng tin 2 cột cạnh nhau (8/4), dải thống kê chồng lên mép dưới hero (`-mt-16`), hàng 4 thẻ ngang (Sản vật/Ban Tự Quản/Tổ ANTT/Dịch vụ công), Gallery (scroll ngang) + Liên kết nhanh 2 cột (8/4), footer 3 cột nền xanh đậm. Đã xác nhận bằng ảnh chụp Puppeteer thật, không chỉ đọc code.
- [x] **Vòng 2 — bổ sung phần tương tác còn thiếu (vẫn theo phản hồi "chưa khớp hoàn toàn")**: `home-page.tsx` chuyển thành Client Component để có đủ hành vi thật của bản mẫu, không chỉ hiển thị tĩnh:
  - Bộ lọc tin tức theo `categorySlug` (Tất cả/Hành chính/Sản xuất/Đoàn thể) + modal xem chi tiết 1 tin (đúng nội dung `content` đầy đủ, không chỉ `summary`).
  - Modal "Xem đầy đủ" (Ban Tự Quản) / "Xem chi tiết" (Tổ ANTT, có sơ đồ phân nhiệm dạng cây: Tổ Trưởng → Tổ Phó → số Tổ viên, đúng cấu trúc `renderHomeSecurity()` gốc).
  - Nút scroll trái/phải cho Hình Ảnh Hoạt Động (dùng `ref` + `scrollBy`).
  - Header desktop: thêm link Fanpage Facebook + ô tìm kiếm (site-wide qua Google `site:` search, giữ đúng hành vi `runSiteSearch()` gốc).
  - **Header + bottom tab bar riêng cho mobile** (trước đó chỉ có 1 layout dùng chung cho mọi kích thước màn hình) — hamburger mở sidebar trượt, thanh điều hướng dưới cùng 5 mục (Trang chủ/Thông báo/nút nổi "Phản ánh ANTT"/Tra cứu/Tài khoản), hero rút gọn riêng cho mobile.
  - Đã xác nhận lại bằng Puppeteer ở cả viewport desktop (1400px) và mobile (420px), cùng chụp ảnh sau khi bấm nút mở modal thật (không chỉ xem code).
- [x] **Trang chủ công khai** (`src/app/home-page.tsx` + `page.tsx`) — dựng theo `index.html` bản mẫu: header+nav, khối giới thiệu+thống kê, tin tức, sản vật địa phương, đường dây nóng ANTT, lịch công tác, hình ảnh hoạt động, footer. Gọi API thật (`/home-content`), không hardcode nội dung. **Lược bớt** so với bản mẫu: carousel mobile JS-driven (thay bằng grid responsive CSS thuần), mục "Ban Tự Quản"/team ANTT (theo ghi chú trong `data.js`, 2 mục này vốn build động từ `accounts`, không phải static content — cần module `residents`/`accounts` ở BE trước, chưa làm ở giai đoạn này).
- [x] **Trang đăng nhập** (`/login`) — dựng lại đúng theo `dang-nhap.html`: logo+tên thôn, card xác thực, **bảng chọn nhanh 5 tài khoản demo** (khớp `defaultAccounts`), gọi BFF thật.
- [x] **Đã test lại toàn bộ end-to-end** sau khi thêm style thật: trang chủ render đủ 6 section với dữ liệu thật, trang login render đúng, cả 2 build sạch.
- [x] **BE: `HouseholdsModule`** (`src/households/`) — `GET /households/me` (trả `familyId`, danh sách thành viên cùng hộ, `gpsCoord`, `houseNumber`, `fundObligations`), `PATCH /households/me/gps`, `PATCH /households/me/house-number`. Mọi endpoint tự resolve "hộ của người đang đăng nhập" qua `Account.residentId` → `Resident.familyId` (không có route đọc hộ người khác — thuộc phạm vi Trưởng thôn/Admin, chưa làm).
- [x] **BE: `RequestsModule`** (`src/requests/`) — `POST /requests/member-edit`, `POST /requests/new-member`, `GET /requests/mine`. Trạng thái lưu bằng slug tiếng Anh (`pending`/`approved`/`rejected`) theo quy ước đặt tên của dự án — FE tự map sang nhãn tiếng Việt hiển thị.
- [x] **Dashboard Cư dân — Tổng quan** (`src/app/dashboard/overview-tab.tsx`) — dựng lại đúng `renderResidentDashboard()`: banner chào mừng, 4 thao tác nhanh, thông báo mới nhất + lịch công tác (từ `/home-content`), tiến độ đóng góp quỹ hộ (từ `/households/me`), "Yêu cầu của tôi" (từ `/requests/mine`, gộp 2 loại request + sort theo `_id` giảm dần vì ObjectId hex tự sắp xếp theo thời gian tạo), sản vật, liên hệ ANTT (tổ trưởng lấy từ `/home-content/public-roster`).
- [x] **Dashboard Cư dân — Hộ gia đình** (`src/app/dashboard/family-tab.tsx` + `family-gps-map.tsx`) — bảng thành viên (badge "Chờ Admin duyệt" nếu có request pending), modal "Sửa thông tin" + "Thêm thành viên" (gọi `POST /requests/*` qua BFF proxy), bản đồ GPS thật (Leaflet + OpenStreetMap) vẽ **ranh giới thật của Thôn Đoàn Kết** (`Tenant.boundary` — cùng nguồn GeoJSON dùng khi migrate-seed, lấy qua `GET /tenants/public`) + marker nhà tại `gpsCoord`, nút "Định vị GPS hiện tại" (browser Geolocation, PATCH `/households/me/gps`), ô Số nhà (PATCH `/households/me/house-number`). **Lược bớt** so với bản mẫu: ranh giới 24 thôn/buôn lân cận làm bối cảnh xung quanh (bản mẫu dùng `js/village-boundary.js` tĩnh ~492KB) — chỉ vẽ ranh giới thôn mình, có thể bổ sung sau bằng cách gọi lại đúng GeoJSON API như `migrate-seed.ts`.
- [x] **Portal shell dùng chung** (`src/app/dashboard/resident-portal.tsx`) — header (logo, hotline, chuông thông báo có badge số yêu cầu đang chờ duyệt, avatar, đăng xuất), sidebar 5 tab (2 tab đã dựng: "Tổng quan Hộ gia đình", "Thành viên & Vị trí GPS"; 3 tab còn lại đánh dấu "Sắp có" — Quỹ Thôn/Báo ANTT/Đăng Ký Lưu Trú), thanh điều hướng dưới cùng cho mobile — theo đúng cấu trúc `dashboard-cu-dan.html` gốc.
- [x] **BFF proxy cho thao tác ghi từ Client Component** (`src/app/api/backend/[...path]/route.ts` + `src/lib/client-api.ts`) — Client Component không có `cookies()`/`headers()` như Server Component nên không gọi thẳng BE được; route handler này đọc cookie `session` phía server rồi forward `PATCH`/`POST` sang BE kèm `x-tenant-slug` + `Authorization`.
- [x] **Sửa bug: `middleware.ts` đặt sai chỗ** — dự án dùng cấu trúc `src/`, nhưng `middleware.ts` lại nằm ở project root → Next.js **không nhận** (chạy như không có middleware nào, `x-tenant-slug` không bao giờ được set, mọi API cần tenant trả 404 "Không tìm thấy thôn này" kể cả khi truy cập đúng subdomain). Không có lỗi build/lỗi console nào báo hiệu — chỉ phát hiện khi debug vì sao `/api/auth/login` trả 404. Đã chuyển vào `src/middleware.ts`. Next.js 16 còn báo file này nên đổi tên thành `proxy.ts` với hàm export `proxy` (quy ước mới thay cho `middleware`) — đã đổi theo, hết cảnh báo.
- [x] **Đã kiểm thử thật bằng Puppeteer**: đăng nhập cư dân `066080004452`/`doanket` → dashboard Tổng quan render đúng dữ liệu thật (đóng góp 750.000đ/850.000đ = 88%, yêu cầu đang chờ duyệt hiện đúng), chuyển tab Hộ gia đình → bảng 5 thành viên đúng, bản đồ Leaflet vẽ đúng ranh giới thật + marker nhà, mở modal "Sửa thông tin" hoạt động đúng (điền sẵn dữ liệu, gửi request thành công, badge "Chờ Admin duyệt" cập nhật ngay). Đã test cả viewport mobile (420px) — header/bottom-nav/nội dung đều đúng.
- [x] **Bổ sung đủ trường cho modal "Sửa thông tin"/"Thêm thành viên"** — ban đầu chỉ có 5 trường (tên/quan hệ/ngày sinh/CCCD/SĐT), thiếu so với bản mẫu (`fatherName`, `motherName`, `group` — chọn 1 trong 5 nhóm cư trú, `permanentAddress`, `temporaryAddress`). Đã bổ sung ở cả 2 lớp: `Resident` schema đổi field `address` thành `permanentAddress` + thêm `temporaryAddress` (migrate lại dữ liệu qua `migrate:seed` + `generate:fake-households`), `EditableMemberFields`/`NewMemberRequest` schema + DTO thêm đủ 5 field mới, `RequestsService.createMemberEditRequest` lấy đủ `oldValues` từ Resident. FE: `family-tab.tsx` 2 modal đều thêm khối cha/mẹ (2 cột), `<select>` nhóm cư trú (`RESIDENT_GROUPS` trong `src/lib/types.ts`), 2 ô địa chỉ, form cuộn được (`max-h-[70vh] overflow-y-auto`) khi nội dung dài — khớp đúng bố cục `request-member-modal`/`add-member-modal` trong `dashboard-cu-dan.html` gốc. Đã xác nhận lại bằng Puppeteer (chụp cả 2 modal đầy đủ trường) + curl (BE nhận và lưu đúng toàn bộ trường mới).
- [x] **Validate bắt buộc/định dạng cho 2 modal (theo yêu cầu cụ thể)** — bắt buộc: Họ và tên, Quan hệ với chủ hộ, Số Căn Cước (đúng 12 chữ số), Giới tính (thêm mới field `gender` vào form — bản mẫu gốc không có field này), Ngày sinh (đổi từ ô nhập chữ tự do sang `<input type="date">`, chuyển đổi qua lại định dạng "DD/MM/YYYY" bằng `dmyToIso`/`isoToDmy`, cùng logic với `js/app-init.js` gốc). Không bắt buộc: cha/mẹ, nhóm cư trú, 2 địa chỉ. Số điện thoại: không bắt buộc, nhưng nếu nhập phải đúng 10 chữ số. Validate ở **cả 2 lớp**: FE (`validateMemberForm()` trong `family-tab.tsx`, chặn submit sớm + hiện lỗi tiếng Việt cụ thể) và BE (`class-validator`: `@Matches(/^\d{12}$/)` cho CCCD, `@Matches(/^$|^\d{10}$/)` cho SĐT, `@IsIn(['male','female'])` cho giới tính) — **phát hiện thêm 1 lỗi khi làm việc này**: DTO `CreateMemberEditRequestDto.newValues` thiếu `@ValidateNested()` + `@Type(() => EditableMemberFieldsDto)` nên `class-validator` **không hề validate** các field lồng bên trong (`newValues`) từ trước tới giờ — mọi giá trị (kể cả rỗng/sai định dạng) đều lọt qua. Đã thêm 2 decorator này để bật validate lồng. `Resident.gender` vẫn giữ enum `['male','female','unknown']` ở tầng lưu trữ (dữ liệu thật migrate từ prototype không có sẵn giới tính nên mặc định `'unknown'`) — chỉ **request** (khi cư dân chủ động sửa/thêm) mới bắt buộc chọn Nam/Nữ. Đã kiểm thử đủ 6 bước bằng Puppeteer (thiếu từng trường lần lượt → đúng thông báo lỗi tương ứng → điền đủ & đúng định dạng → gửi thành công).
- [ ] Các dashboard còn lại theo role (village-head/association-officer/security-team/admin) — BE mới có auth+tenant+home-content+households+requests, chưa có module cho funds/incidents/residence-registration/associations nên các tab "Sắp có" (Quỹ Thôn/Báo ANTT/Đăng Ký Lưu Trú) của Cư dân và toàn bộ dashboard của 4 role còn lại chưa dựng

**Lưu ý đặt tên:** dự án rebuild dùng tên code (route/file/biến) tiếng Anh chuẩn chuyên nghiệp, khác với bản prototype gốc (tiếng Việt). Nội dung hiển thị cho người dùng (label, tiêu đề, text) vẫn tiếng Việt — chỉ đổi tên ở tầng code.

## Giai đoạn 3 — Superadmin ✅ HOÀN THÀNH (phần CRUD tenant)

- [x] `SuperAdminGuard` (`src/common/guards/superadmin.guard.ts`) — JWT riêng biệt, không gắn tenant, phân biệt bằng `payload.scope === 'superadmin'`
- [x] **BE `SuperAdminModule`** (`src/superadmin/`) — `SuperAdminAuthService`/`Controller` (`POST /superadmin/auth/login`, JWT riêng scope `superadmin`, dùng chung `JwtModule` từ `AuthModule`), `SuperAdminTenantsService`/`Controller`:
  - `GET /superadmin/tenants` — mọi tenant kể cả đã khóa, kèm `accountCount`/`residentCount` (thống kê cơ bản, mục 8.6)
  - `GET /superadmin/tenants/:id`
  - `POST /superadmin/tenants` — tạo `Tenant` mới **+ 1 Account admin đầu tiên** trong cùng 1 thao tác (slug trùng → 409)
  - `PATCH /superadmin/tenants/:id` — đổi tên, hoặc khóa/mở (`{archived: true/false}` → set/xóa `Tenant.archivedAt`, soft-delete cấp tenant đúng mục 7 tài liệu thiết kế — đã xác nhận `TenantGuard` trả 404 khi tenant bị khóa, không xóa dữ liệu)
- [x] **Script seed** `scripts/seed-superadmin.ts` (`npm run seed:superadmin`) — tạo tài khoản superadmin đầu tiên (mặc định `superadmin`/`superadmin`, đổi qua biến môi trường `SUPERADMIN_USERNAME`/`SUPERADMIN_PASSWORD`), idempotent (chạy lại chỉ đổi mật khẩu).
- [x] **FE khu vực superadmin** (`src/app/superadmin/`) — **đơn giản hoá so với tài liệu thiết kế**: dùng route `/superadmin/login` + `/superadmin` ở **domain gốc theo path**, không dùng subdomain riêng `admin.tenxa.vn` như mục 5 tài liệu thiết kế đề xuất (subdomain riêng cần thêm logic đặc cách trong `proxy.ts` để không bị hiểu nhầm thành tenant slug "admin" — để lại cho giai đoạn hạ tầng VPS thật, lúc đó cấu hình DNS/nginx thật cho `admin.tenxa.vn`). Cookie httpOnly riêng `superadmin_session` (khác `session` của tenant), BFF riêng (`/api/superadmin/auth/login|logout`) + proxy ghi riêng (`/api/superadmin-backend/[...path]`) — tách biệt hoàn toàn khỏi luồng cookie/proxy của cư dân.
  - Trang đăng nhập riêng (theme tối, phân biệt trực quan với đăng nhập cư dân).
  - Dashboard: bảng danh sách tenant (tên/slug/số tài khoản/số nhân khẩu/trạng thái), nút Khóa/Mở khóa, modal "Tạo tenant mới" (slug + tên + tài khoản admin đầu tiên).
- [x] **Nhập KMZ theo Xã + tạo tenant bằng cách bấm chọn thôn trên bản đồ** (tính năng phát sinh thêm trong phiên, thay cho ý tưởng ban đầu "upload KMZ điền sẵn ranh giới cho 1 tenant"):
  - **BE**: `Commune` schema mới (`src/schemas/commune.schema.ts`) — 1 bản ghi Commune = 1 lần nhập KMZ của 1 xã, chứa mảng `villages[]` (mỗi Placemark polygon trong KMZ = 1 thôn: tên, slug gợi ý tự sinh không dấu, tọa độ tâm, ranh giới GeoJSON, trạng thái `claimed`/`tenantId` sau khi đã tạo tenant). `src/superadmin/kmz.util.ts` tự giải nén KMZ (thư viện `adm-zip`) và trích xuất TẤT CẢ polygon + tên bằng regex (không cần parser XML đầy đủ) — tự động bỏ qua Placemark chỉ có 1-2 tọa độ (điểm mốc/nhãn, không phải ranh giới thật).
  - **API mới**: `POST /superadmin/communes` (multipart: field `file` + `name`) — nhập KMZ, tạo Commune; `GET /superadmin/communes` — danh sách xã kèm tổng số thôn/số đã tạo tenant; `GET /superadmin/communes/:id` — chi tiết (cho bản đồ); `POST /superadmin/communes/:id/villages/:index/create-tenant` — tạo Tenant thật từ 1 thôn cụ thể (dùng lại `SuperAdminTenantsService.create()`, tự điền `lat/lng/boundary` từ thôn đã chọn), đánh dấu thôn đó `claimed` (409 nếu bấm tạo lại thôn đã claimed).
  - **Quyết định kiến trúc quan trọng** (sau khi trao đổi lại phạm vi với bạn): ban đầu định làm mỗi Xã gắn 1 domain gốc riêng (`Commune.rootDomain`) — **đã bỏ**, vì tầng BE/superadmin vốn dĩ không gắn domain nào (JWT scope `superadmin` không chứa thông tin domain), nên **1 superadmin vẫn quản lý được nhiều xã/nhiều domain bình thường** mà không cần ràng buộc Commune↔domain. Thay vào đó chỉ cần tổng quát hóa **`src/proxy.ts`** (middleware) — bỏ hằng số `ROOT_DOMAIN="tenxa.vn"` cứng, đổi sang xét **số nhãn của hostname** (2 nhãn = domain gốc/không subdomain, >2 nhãn = có subdomain, nhãn đầu là tenant slug) — nhờ vậy bất kỳ domain thật nào trỏ vào cùng FE/BE đều tự hoạt động đúng, không cần sửa code hay cấu hình gì thêm cho từng xã.
  - **FE**: `src/app/superadmin/communes/` — trang danh sách Xã (bảng + modal nhập KMZ, upload file thật qua `FormData`), `src/app/superadmin/communes/[id]/` — trang bản đồ Xã (Leaflet, `commune-leaflet.tsx` là Client Component tách riêng dynamic-import `ssr:false` giống pattern `FamilyGpsMap`): thôn chưa có tenant tô cam + bắt sự kiện click, thôn đã có tenant tô xanh (chỉ xem popup). Route ghi multipart riêng `src/app/api/superadmin-backend/communes/route.ts` (forward nguyên stream + Content-Type kèm boundary sang BE, `duplex:'half'`) vì proxy chung `[...path]` chỉ xử lý được JSON.
  - **Điều chỉnh theo phản hồi ("không có tạo tenant mà chỉ có sửa, rồi chọn nó thuộc xã nào")**: bấm 1 thôn chưa có tenant trên bản đồ giờ **chuyển trang** (không phải mở modal) sang `src/app/superadmin/communes/[id]/villages/[index]/` — trang "sửa thông tin thôn" đầy đủ hơn (chỉnh lại tên/slug lấy từ KMZ trước khi tạo tài khoản admin), thay vì modal nhanh trước đây.
  - **Thêm khả năng gán/bỏ gán Xã-Thôn cho tenant ĐÃ CÓ SẴN** (không chỉ tenant mới tạo từ bản đồ): `Tenant` schema thêm `communeId`/`communeVillageIndex` (optional); `PATCH /superadmin/tenants/:id/assign-village` (`SuperAdminTenantsService.assignVillage()`) — gán tenant vào 1 thôn cụ thể (đồng bộ lại `lat/lng/boundary` của tenant theo thôn đã chọn, đánh dấu thôn đó `claimed`), tự động **bỏ claimed thôn cũ** nếu tenant đổi sang thôn khác, chặn gán trùng vào thôn đã có tenant khác (409), hỗ trợ bỏ gán hoàn toàn (`communeId: null`). FE: trang danh sách Tenant chính (`/superadmin`) có thêm cột "Thuộc xã" + nút "Sửa" mở modal chọn Xã → chọn Thôn (danh sách thôn của xã đó, disable các thôn đã bị tenant khác chiếm) → Lưu.
  - Đã kiểm thử end-to-end bằng curl (import KMZ giả lập 2 Placemark polygon + 1 điểm mốc → xác nhận điểm mốc bị loại đúng → tạo tenant từ 1 thôn → xác nhận `claimed`/`tenantId` cập nhật đúng → bấm tạo lại thôn đã claimed → đúng 409; gán tenant có sẵn vào thôn → đổi sang thôn khác → xác nhận thôn cũ tự bỏ claimed → bỏ gán hoàn toàn → xác nhận thôn được thả tự do) và Puppeteer thật cho cả 2 luồng (nhập KMZ qua UI → bấm polygon → trang sửa thôn → tạo tenant thành công; và mở "Sửa" từ danh sách tenant → chọn xã → chọn thôn → Lưu → cột "Thuộc xã" cập nhật đúng). Dữ liệu test đã dọn sạch, xác nhận lại tenant "doanket" không còn communeId thừa và ranh giới thật (từ `migrate-seed.ts`) không bị ghi đè bởi dữ liệu test.
- [x] **Đã kiểm thử end-to-end**: curl (login → tạo tenant+admin → khóa → xác nhận `/auth/login` vào tenant đó trả 404 → mở lại → login thành công) và Puppeteer (đăng nhập superadmin thật, mở modal tạo tenant, điền form, xác nhận tenant mới hiện trong bảng, bấm khóa → badge chuyển "ĐÃ KHÓA" + thông báo thành công). Dữ liệu test đã dọn sạch sau khi xác nhận.
- [ ] CRUD `AdministrativeUnit` qua route superadmin (`POST/GET/PATCH /superadmin/administrative-units`) — chưa làm, hiện trụ sở cơ quan xã chỉ có được qua `migrate-seed.ts`, chưa có API quản lý riêng
- [x] **Trang danh mục công khai ở domain gốc — mục 4.1 tài liệu thiết kế (cuối cùng đã dựng UI thật)**: trước đây chỉ là placeholder text, giờ dùng chính dữ liệu `Commune` (nhập KMZ qua superadmin) để hiện bản đồ tra cứu thật, theo đúng bố cục `tra-cuu.html` gốc (header tên xã + tagline, ô tìm kiếm, bản đồ + danh sách 2 cột).
  - **BE**: module `src/communes/` mới (public, không cần đăng nhập, tách riêng khỏi `src/superadmin/`) — `GET /communes/public` trả mọi Commune kèm danh sách thôn, mỗi thôn kèm `tenantSlug`/`tenantName` NẾU đã được gán tenant (qua `create-tenant`/`assign-village`) **và** tenant đó chưa bị khóa (`archivedAt: null` — tenant bị khóa tự động "biến mất" khỏi bản đồ công khai, không cần xóa gán).
  - **FE**: `src/app/page.tsx` ở nhánh domain gốc (không có `x-tenant-slug`) gọi `/communes/public` thay cho placeholder cũ; `src/app/directory/directory-client.tsx` (danh sách/tìm kiếm/thống kê "x/y thôn đã có cổng thông tin") + `directory-leaflet.tsx` (Leaflet, dynamic `ssr:false`) — thôn đã có tenant tô xanh (bấm vào hoặc bấm dòng trong danh sách đều điều hướng sang đúng subdomain), thôn chưa có tô xám (không bấm được). Điều hướng dùng `window.location.host` hiện tại (không hardcode tên miền) — tự hoạt động đúng trên bất kỳ domain thật nào, nhất quán với việc tổng quát hóa `proxy.ts` ở trên.
  - Đã kiểm thử end-to-end bằng Puppeteer thật: trang chủ domain gốc hiện đúng bản đồ + danh sách, gõ tìm kiếm lọc đúng danh sách, **bấm vào 1 thôn đã có tenant → điều hướng thành công sang đúng subdomain** (`http://doanket.localhost:3001/`). Dữ liệu test đã dọn sạch.
  - **Vòng 2 — bám sát `tra-cuu.html` hơn + sửa 2 lỗi phát hiện qua phản hồi**: viết lại `directory-client.tsx`/`directory-leaflet.tsx` theo đúng màu sắc/bố cục bản mẫu (nền gradient xanh nhạt, brand nâu cam `#9c3000`, ô tìm kiếm với gợi ý sổ xuống, layout bản đồ + panel "Chi tiết"/"Danh sách" 2 tầng, danh sách đánh số tròn, thôn đang chọn viền đỏ hồng + bản đồ tự bay tới `flyToBounds`). Phát hiện thêm 1 lỗi z-index cùng loại với modal trước đó: hộp gợi ý tìm kiếm (`z-[2000]`) vẫn bị bản đồ Leaflet che khuất vì **div cha** chỉ có `z-[50]` — z-index của phần tử con chỉ so sánh được trong stacking context của cha, nên cha thấp thì con cao mấy cũng vô nghĩa với anh em khác cha; đã sửa bằng cách nâng z-index của div cha lên `z-[1100]`. **Theo phản hồi "1 miền xã chỉ hiện 1 xã"**: `DirectoryClient` trước đó lặp qua **mọi** Commune và xếp chồng từng xã lên nhau trên cùng 1 trang — sửa lại chỉ lấy `communes[0]` (xã nhập KMZ gần nhất, do BE đã sort `createdAt desc`), đúng nguyên tắc 1 domain phục vụ 1 xã (nhiều xã/nhiều domain vẫn quản lý bình thường ở tầng superadmin, chỉ trang công khai này rút gọn còn 1 xã). Đã build lại + kiểm thử bằng Puppeteer xác nhận gợi ý tìm kiếm hiện đúng trên bản đồ.

## Giai đoạn 4 — Hạ tầng VPS (chưa bắt đầu)

- [ ] nginx, wildcard SSL, PM2, MongoDB backup

---
**Cách chạy lại từ đầu:**
```
# BE (port 3000)
cd E:\Dev\cong-thong-tin-thon-be
npm run start:dev
npm run migrate:seed        # reset dữ liệu tenant "doanket" nếu cần

# FE (port khác, vd 3001 — BE đã chiếm 3000)
cd E:\Dev\cong-thong-tin-thon-fe
npm run dev -- -p 3001
# Truy cập: http://doanket.localhost:3001 (đăng nhập admin/doanket, hoặc 066080004452/doanket cho Cư dân)
#           http://localhost:3001 (trang danh mục — chưa có subdomain)
```
**Đã sửa (trước đây có ghi chú lệch slug):** từng có lúc DB chứa tenant slug `"doan-ket"` (có gạch nối) trong khi `TENANT_SLUG` trong `migrate-seed.ts` là `"doanket"` — script xóa-trước-khi-tạo tìm theo slug mới nên không dọn được doc slug cũ, để lại 2 tenant trùng tên. Đã dọn sạch tenant/dữ liệu cũ theo `"doan-ket"`, chạy lại `migrate:seed`, giờ chỉ còn đúng 1 tenant slug `"doanket"` — luôn dùng đúng slug này (không có gạch nối) khi truy cập subdomain hoặc test.
